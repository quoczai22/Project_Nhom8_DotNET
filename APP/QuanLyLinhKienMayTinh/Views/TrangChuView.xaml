<Page x:Class="QuanLyLinhKienMayTinh.Views.TrangChuView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:QuanLyLinhKienMayTinh"
      xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
      xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
      mc:Ignorable="d" 
      d:DesignHeight="720" d:DesignWidth="1000"
      Title="TrangChu">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <UniformGrid Grid.Row="0" Columns="5">
            <Border Style="{StaticResource DashboardCardStyle}">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <Border Background="#e8eaf6" CornerRadius="10" Padding="8" Margin="0,0,10,0">
                            <Icon:PackIconMaterial Kind="AccountTie" Foreground="#3f51b5"/>
                        </Border>
                        <TextBlock Text="Tổng Nhân Viên" Style="{StaticResource CardTitleStyle}" VerticalAlignment="Center"/>
                    </StackPanel>
                    <TextBlock Text="{Binding TongNV}" Style="{StaticResource CardValueStyle}"/>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource DashboardCardStyle}">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <Border Background="#e3f2fd" CornerRadius="10" Padding="8" Margin="0,0,10,0">
                            <Icon:PackIconMaterial Kind="AccountGroup" Foreground="#2196f3"/>
                        </Border>
                        <TextBlock Text="Khách Hàng" Style="{StaticResource CardTitleStyle}" VerticalAlignment="Center"/>
                    </StackPanel>
                    <TextBlock Text="{Binding TongKH}" Style="{StaticResource CardValueStyle}"/>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource DashboardCardStyle}">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <Border Background="#f3e5f5" CornerRadius="10" Padding="8" Margin="0,0,10,0">
                            <Icon:PackIconMaterial Kind="Memory" Foreground="#9c27b0"/>
                        </Border>
                        <TextBlock Text="Linh Kiện" Style="{StaticResource CardTitleStyle}" VerticalAlignment="Center"/>
                    </StackPanel>
                    <TextBlock Text="{Binding TongLK}" Style="{StaticResource CardValueStyle}"/>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource DashboardCardStyle}">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <Border Background="#fff3e0" CornerRadius="10" Padding="8" Margin="0,0,10,0">
                            <Icon:PackIconMaterial Kind="TagMultiple" Foreground="#ff9800"/>
                        </Border>
                        <TextBlock Text="Loại Linh Kiện" Style="{StaticResource CardTitleStyle}" VerticalAlignment="Center"/>
                    </StackPanel>
                    <TextBlock Text="{Binding TongLoaiLK}" Style="{StaticResource CardValueStyle}"/>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource DashboardCardStyle}">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <Border Background="#e8f5e9" CornerRadius="10" Padding="8" Margin="0,0,10,0">
                            <Icon:PackIconMaterial Kind="Receipt" Foreground="#4caf50"/>
                        </Border>
                        <TextBlock Text="Tổng Hóa Đơn" Style="{StaticResource CardTitleStyle}" VerticalAlignment="Center"/>
                    </StackPanel>
                    <TextBlock Text="{Binding TongHD}" Style="{StaticResource CardValueStyle}"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <Grid Grid.Row="1" Margin="10,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource DashboardCardStyle}" Margin="0,10,10,10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Thống kê Doanh thu / Bán chạy nhất" Style="{StaticResource CardTitleStyle}" Margin="0,0,0,10"/>
                    <lvc:CartesianChart Grid.Row="1" Series="{Binding DoanhThu}">
                        <lvc:CartesianChart.AxisX>
                            <lvc:Axis Labels="{Binding Labels}" />
                        </lvc:CartesianChart.AxisX>
                    </lvc:CartesianChart>
                </Grid>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource DashboardCardStyle}" Margin="10,10,0,10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Nhân viên theo chức vụ" Style="{StaticResource CardTitleStyle}" Margin="0,0,0,10"/>
                    <lvc:PieChart Grid.Row="1" Series="{Binding ChucVu}" InnerRadius="40" LegendLocation="Bottom" />
                </Grid>
            </Border>
        </Grid>

        <Border Grid.Row="2" Style="{StaticResource DashboardCardStyle}" Margin="10,0,10,10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Số lượng Linh kiện theo Hãng" Style="{StaticResource CardTitleStyle}" Margin="0,0,0,10"/>

                <DataGrid Grid.Row="1" ItemsSource="{Binding DanhSachThongKeBanHang}"
                          AutoGenerateColumns="False" IsReadOnly="True" 
                          Background="White" BorderThickness="1" BorderBrush="#e0e0e0"
                          HeadersVisibility="Column" GridLinesVisibility="Horizontal">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Số lượng" Binding="{Binding SoLuong}" Width="150">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="Hãng" Binding="{Binding HangSX}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>

            </Grid>
        </Border>
    </Grid>
</Page>