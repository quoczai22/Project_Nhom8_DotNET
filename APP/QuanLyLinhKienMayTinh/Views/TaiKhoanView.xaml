<Page x:Class="QuanLyLinhKienMayTinh.Views.TaiKhoanView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:QuanLyLinhKienMayTinh.Views"
        mc:Ignorable="d"
        Title="TaiKhoanView" Height="450" Width="800" Loaded="Page_Loaded">
    <StackPanel Margin="20" Width="400" HorizontalAlignment="Left">
        <TextBlock Text="Chọn nhân viên (chưa có tài khoản):"/>
        <ComboBox ItemsSource="{Binding DSNhanVienKhongTaiKhoan}" 
       DisplayMemberPath="TenNv" 
       SelectedItem="{Binding SelectedNhanVien, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Text="Chức vụ của nhân viên:"/>
        <TextBox Text="{Binding ChucVuHienTai, Mode=OneWay}" IsReadOnly="True" Background="#eee" Foreground="#555" Margin="0,5,0,15"/>

        <TextBlock Text="Tên đăng nhập (Username):"/>
        <TextBox Text="{Binding TenDangNhapMoi}" Margin="0,5,0,15"/>

        <TextBlock Text="Mật khẩu (Password):"/>
        <TextBox Text="{Binding MatKhauMoi}" Margin="0,5,0,15"/>

        <Button Content="TẠO TÀI KHOẢN VÀ CẤP QUYỀN" Command="{Binding TaoTaiKhoanCommand}" Height="40" Background="#43a047" Foreground="White" FontWeight="Bold"/>
    </StackPanel>
</Page>
