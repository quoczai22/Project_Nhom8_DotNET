<Window x:Class="QuanLyLinhKienMayTinh.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
        mc:Ignorable="d"
        Title="Quản Lý Linh Kiện Máy Tính" Height="720" Width="1200"
        Background="WhiteSmoke"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border x:Name="SidebarBorder" 
                Grid.Column="0" 
                Background="#3f51b5" 
                CornerRadius="0,20,20,0" 
                Panel.ZIndex="10">
            <Border.Style>
                <Style TargetType="Border" BasedOn="{StaticResource SidebarAnimationStyle}">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsMouseOver, ElementName=btnHoverMenu}" Value="True">
                            <Setter Property="Tag" Value="Open"/>
                        </DataTrigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Tag" Value="Open"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>

            <DockPanel>
                <StackPanel DockPanel.Dock="Top" Margin="0,30,0,30">
                    <Ellipse Width="60" Height="60" Margin="0,0,0,10">
                        <Ellipse.Fill>
                            <ImageBrush ImageSource="/Images/avatar.png"/>
                        </Ellipse.Fill>
                    </Ellipse>
                    <TextBlock Text="{Binding CurrentUsername}" Foreground="White" 
                               FontSize="18" FontWeight="Bold" HorizontalAlignment="Center"/>
                </StackPanel>

                <Button DockPanel.Dock="Bottom" Style="{StaticResource menuButton}" 
                        Command="{Binding DangXuatCommand}" 
                        CommandParameter="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type Window}}}" 
                        Margin="10,0,10,20">
                    <StackPanel Orientation="Horizontal">
                        <Icon:PackIconMaterial Kind="Logout" Style="{StaticResource menuButtonIcon}"/>
                        <TextBlock Text="Đăng xuất" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <StackPanel>
                    <Button Style="{StaticResource menuButton}" Click="btnDashboard_Click">
                        <StackPanel Orientation="Horizontal">
                            <Icon:PackIconMaterial Kind="HomeVariant" Style="{StaticResource menuButtonIcon}"/>
                            <TextBlock Text="Trang Chủ" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource menuButton}" Click="btnLinhKien_Click">
                        <StackPanel Orientation="Horizontal">
                            <Icon:PackIconMaterial Kind="PackageVariant" Style="{StaticResource menuButtonIcon}"/>
                            <TextBlock Text="Linh Kiện" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource menuButton}" Click="btnLoaiLK_Click">
                        <StackPanel Orientation="Horizontal">
                            <Icon:PackIconMaterial Kind="FormatListBulletedType" Style="{StaticResource menuButtonIcon}"/>
                            <TextBlock Text="Loại Linh Kiện" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource menuButton}" Click="btnKhachHang_Click">
                        <StackPanel Orientation="Horizontal">
                            <Icon:PackIconMaterial Kind="AccountGroup" Style="{StaticResource menuButtonIcon}"/>
                            <TextBlock Text="Khách Hàng" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource menuButton}" Click="btnNhanVien_Click">
                        <StackPanel Orientation="Horizontal">
                            <Icon:PackIconMaterial Kind="FileDocumentOutline" Style="{StaticResource menuButtonIcon}"/>
                            <TextBlock Text="Nhân viên" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource menuButton}" Click="btnHoaDon_Click">
                        <StackPanel Orientation="Horizontal">
                            <Icon:PackIconMaterial Kind="FileDocumentOutline" Style="{StaticResource menuButtonIcon}"/>
                            <TextBlock Text="Hóa Đơn" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </DockPanel>
        </Border>

        <Grid Grid.Column="1" Margin="30,20,30,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,10,0,30">
                <Button x:Name="btnHoverMenu" Style="{StaticResource topButton}" 
                        HorizontalAlignment="Left" Margin="0,0,0,0">
                    <Icon:PackIconMaterial Kind="Menu" Width="15" Height="15"/>
                </Button>

                <Grid Width="300" HorizontalAlignment="Left" Margin="50,0,0,0">
                    <TextBlock Margin="20,0" VerticalAlignment="Center" Foreground="#b0b9c6" 
                               IsHitTestVisible="False" Panel.ZIndex="1" Text="Search here ..."
                               Visibility="{Binding ElementName=txtSearch, Path=Text.IsEmpty, Converter={StaticResource BoolToVis}}"/>
                    <TextBox x:Name="txtSearch" Style="{StaticResource textboxSearch}"/>
                    <Icon:PackIconMaterial Kind="Magnify" Width="15" Height="15" VerticalAlignment="Center"
                                           Margin="0,0,15,0" Foreground="#b0b9c6" HorizontalAlignment="Right"/>
                </Grid>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="1070,0,0,0">
                    <Button Style="{StaticResource topButton}" Margin="0,0,10,0" Command="{Binding ThongBaoCommand}">
                        <Icon:PackIconMaterial Kind="BellOutline" Width="18" Height="18" Foreground="#6e7783"/>
                    </Button>
                </StackPanel>
            </Grid>

            <Border Grid.Row="2" Background="White" CornerRadius="20" Padding="20">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="15" Direction="-90" ShadowDepth="10" Opacity="0.1"/>
                </Border.Effect>
                <Frame x:Name="MainFrame" NavigationUIVisibility="Hidden" />
            </Border>
        </Grid>
    </Grid>
</Window>